import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pdm-projeto-firebase',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
